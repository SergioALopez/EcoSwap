<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecoswap">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.vendor.QrReaderActivity"></activity>
        <activity android:name=".ui.user.CuponActivity" />
        <activity android:name=".ui.user.PromotionActivity" />
        <activity android:name=".ui.vendor.EditActivity" />
        <activity android:name=".ui.vendor.DeleteActivity" />
        <activity android:name=".ui.vendor.VendActivity" />
        <activity android:name=".ui.vendor.AddActivity" />
        <activity android:name=".ui.admin.AdminActivity" />
        <activity android:name=".ui.user.HomeActivity" />
        <activity android:name=".ui.vendor.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>